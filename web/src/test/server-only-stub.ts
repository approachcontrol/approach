// `server-only` throws on import outside a React Server Component graph, which
// is exactly its point — and exactly why unit tests need it stubbed out.
// vitest.config.mts aliases the package here.
export {}
